<Window x:Class="viragShop.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:viragShop"
        mc:Ignorable="d"
        Title="Login" Height="350" Width="400" Background="DarkOliveGreen">
    <Window.Resources>
        <ResourceDictionary Source="Resources/Buttons.xaml" />
    </Window.Resources>
    <StackPanel Margin="20" Background="DarkSeaGreen">
        <TextBlock
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        FontSize="25"
        FontWeight="Bold"
        Text="Virag bejelentkezés" />
        <Grid VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
            Grid.Row="0"
            Margin="40,24,0,0"
            HorizontalAlignment="Left"
            Text="felhasználónév" />
            <TextBox
            x:Name="loginUserTxt"
            Grid.Row="1"
            Margin="40,0,0,0"
            Width="250"
            HorizontalAlignment="Left" />
            <TextBlock
            Grid.Row="2"
            Margin="40,0,0,0"
            HorizontalAlignment="Left"
            Text="Jelszó:" />
            <PasswordBox
            x:Name="loginPasswordText"
            Grid.Row="3"
            Margin="40,0,0,0"
            Width="250"
            HorizontalAlignment="Left" />
        </Grid>
        <TextBlock
        HorizontalAlignment="Right"
        Margin="0,10,40,0"
        FontSize="14"
        FontStyle="Italic"
        Foreground="CadetBlue"
        Text="Elfelejtett jelszó" />
        <Button
        x:Name="loginBtn"
        Width="250"
        Height="45"
        Margin="0,50,0,0"
        Click="loginBtn_Click"
        Content="Bejelentkezés"
        Style="{StaticResource ResourceKey=buttons}"/>
    </StackPanel>
</Window>
